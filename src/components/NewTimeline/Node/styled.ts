import styled from "styled-components";

export const NodeStyled = styled.div`
  background-color: var(--active);
  height: 16px;
  width: 16px;
  border-radius: 50%;
  cursor: pointer;
  border: 5px solid var(--lies);
  position: absolute;
`;
