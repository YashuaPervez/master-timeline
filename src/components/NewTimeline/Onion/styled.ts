import styled from "styled-components";

export const OnionStyled = styled.div`
  height: 20px;
  background-color: var(--active);
  border-radius: 3px;
  border: 1px solid #000;
  color: #000;
  position: absolute;
`;
